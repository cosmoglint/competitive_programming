#include <iostream>

using namespace std

int main(){
  int arr [8] = {-1,2,4,-3,5,2,-5,2};
  int sums = 0;
  int ans = 0;
  for (int k = 0; k < n ; k++){
    sums = max(array[k],sum+array[k]);
    best = mx(best, sums);
  }
}
